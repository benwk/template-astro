---
import { languages } from "../i18n/config";
import { getLangFromUrl, removeLangPrefix, useTranslatedPath } from "../i18n";
const lang = getLangFromUrl(Astro.url);
const path = removeLangPrefix(Astro.url.pathname, lang);
const translatePath = useTranslatedPath();
---

<ul>
  {
    Object.entries(languages).map(([language, label]) => (
      <li>
        <a
          href={translatePath(path, language)}
          rel="prefetch"
          class={lang === language ? "active" : ""}
        >
          {label}
        </a>
      </li>
    ))
  }
</ul>

<style>
  .active {
    color: red;
  }
</style>
